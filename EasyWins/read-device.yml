#
# Reads ios_fact and ios_command a from a networking device
# 
# Include it in an Ansible play
#
---
- ios_facts:
    gather_subset: all
- ios_command:
    commands: show bootvar
  register: bootvar
